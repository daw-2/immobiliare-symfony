{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nos annonces</h1>

    <ul>
        {% for property in properties %}
            <li>
                <a href="{{ path('property_show', {id: property.id}) }}">
                    {{ property.name }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
