{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5 mb-5">
        <h1 class="text-center mb-5">Créer une annonce</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button formnovalidate class="btn btn-primary">
                Ajouter l'annonce
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}
